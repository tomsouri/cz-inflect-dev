"""
def remove_suffix(base, suffix):
	
	Works exactly as str.removesuffix, but does not need support of python3.6 or higher.
	
	if base.endswith(suffix):
		return base[:len(base) - len(suffix)]
	else:
		return base
"""
